---
title: useStatus
date: '2022-12-29'
---

Persist the current session status in session storage so that it remains after a page refresh. Can be used to conditionally render a progress bar component, loading view or content based on the status state. Can be extended to handle more states.

Related hooks:

- [`useSessionStorage()`](/react-hook/use-session-storage)
